# CHANGELOG

## Unreleased

+ [安全] 用户端彻底移除 API Key 直连能力：所有模型请求固定经同域代理，代理失败不再回退浏览器直连，上游密钥仅由后台服务端注入。
+ [新增] AI 生成接入服务端积分闭环：按模型计价原子预扣，未配置价格或余额不足时拒绝调用，上游失败自动退款，并记录用户、模型、状态、耗时与积分消耗。
+ [新增] 增加后台模型使用日志，记录模型、接口路径、状态码、耗时和错误摘要，不保存密钥或请求正文。
+ [修复] 平台渠道模式下客户端没有密钥时强制经同域代理请求，避免旧的浏览器直连配置向中转发送空 Authorization；代理继续由服务端按渠道注入密钥。
+ [修复] 同域 AI 代理按目标地址区分鉴权方式：Google 官方 Gemini 域名使用 `x-goog-api-key`，普通 OpenAI 兼容渠道继续使用 `Authorization: Bearer`，避免官方接口返回 `Invalid API Key`。
+ [安全] 前端调试日志与错误摘要不再展示上游完整请求地址，统一仅显示接口路径，避免暴露渠道域名和端口。
+ [修复] Gemini 原生生图请求通过同域代理失败时增加受控直连回退，降低代理 500 导致的生图失败；尺寸参数保持一致。
+ [适配] Gemini Image 与 Nano Banana 系列统一使用 Gemini 生图尺寸参数，支持 1K/2K/4K 和十种宽高比；修复 `/v1` 转 `/v1beta` 时端口后缺少斜杠，原生生成接口不可用时回退 OpenAI `chat/completions` 兼容协议。
+ [修复] 模型渠道拉取模型时自动探测 `/models` 与 `/v1/models`，根域名配置命中 `/v1` 后自动保存规范化 Base URL。
+ [优化] 模型渠道新增“保存并拉取模型”流程，新增渠道无需先关闭弹窗；模型拉取失败保留渠道并展示上游错误，支持列表内重新拉取。
+ [优化] 重构用户个人中心为账户与充值中心，增加积分汇总、套餐购买流程、支付状态引导、流水筛选、刷新与空/错误状态反馈。
+ [优化] 重构管理后台数据概览：增加 7/30 天经营趋势、今日核心指标、积分健康度、套餐表现、模型计价覆盖率、渠道配置状态和可操作的待处理事项；未接入生成任务日志前不展示虚假的上游成功率与延迟。
+ [修复] 支付配置保存按钮改为主动校验和提交，增加保存中状态、成功/失败提示及保存后状态刷新。
+ [优化] 首页中央标题标语增加动态轮播翻转动效：每隔 3 秒丝滑切换多组契合产品核心特性的标语与联动说明（连续生长、自动成片、自由连线、鲜活落地），结合渐变高光与衬线字体，增强视觉动感。
+ [重构] 抛弃旧版完全按腾讯设计 ARDOT (ardot.tencent.com) 风格重构首页：极简中央大字 Hero + 漂浮彩色气泡与迷你节点卡、全宽渐变拟真画布演示容器、三张错落软渐变浮动能力卡、蓝/青柠/抹茶三特性彩色大卡、粉彩安心托付卡与极简大灰字页脚；全页文案极限精简并配备浮动循环、滚动渐入与悬浮动效。
+ [清理] 移除用户端自定义渠道配置及相关抽屉：配置弹窗收敛为模型偏好、生成偏好、提示词来源与 WebDAV 同步，渠道统一由平台管理；顶栏 Key 外链调整为开发文档入口，去除各视图中冗余的渠道名标签。
+ [新增] 支付配置增加微信支付/支付宝独立启用开关，充值页和服务端下单校验同步生效。
+ [优化] 重新设计后台支付配置页面，增加配置完整度摘要、沙箱/生产环境切换、商户与网关分区、生产风险提示和安全的多行密钥录入。
+ [修复] 后台接口返回登录失效时自动清理旧会话，避免“其他设置”等页面一直显示加载中。
+ [优化] 重新设计后台“其他设置”页面，增加策略摘要、账号注册、默认生成积分与高风险维护模式分区，并改为页面内统一保存。
+ [新增] 管理后台顶部增加白色/深色模式切换，并复用全局持久化主题配置。
+ [重构] 上游模型渠道和 API Key 改为管理员后台统一托管，普通用户端不再配置或保存密钥；同域代理按渠道注入密钥，模型获取与模型计价使用同一平台模型清单。
+ [新增] 管理后台支付配置、其他设置和模型计价配置页面；支付密钥仅服务端保存并以配置状态展示，支付运行时读取数据库覆盖环境默认值。
+ [适配] 抽离 cang-ai 的随行付/天阙 RSA 支付协议到 Go 业务服务，增加扫码下单、回调验签、重复通知幂等和服务端环境变量配置。
+ [新增] 接入 Pro Components 后台控制台、会员登录注册、个人积分中心、积分流水查询、套餐管理和模型计价管理页面。
+ [新增] 启动会员积分系统第一阶段：增加 Go + Gin + GORM 业务服务，提供注册登录、JWT 鉴权、用户钱包、积分流水和管理员人工调账基础能力。
+ [规划] 明确积分套餐、支付订单和生成计费采用服务端可信数据、幂等入账，以及预扣、结算、失败退款流程。
+ [修复] 模型用途配置将 MiniMax H3 全系列（含 `minimax-h3-f`）正确识别为视频模型。
+ [适配] MiniMax H3 普通参考模式统一支持最多 9 张图片和 3 段音频，并校验每段音频时长为 1-15 秒。
+ [新增] 画布支持节点分组、组引用展开、分组复制/拖动/删除联动，以及视频任务刷新恢复和首帧、尾帧、当前帧截取。
+ [优化] WebDAV 画布同步增加删除墓碑，避免已删除项目被旧端重新合并；Canvas Agent 收紧本地配置权限、请求 token 传递与日志脱敏。
+ [适配] 772.ee 旧版和新版异步视频接口：按模型类别选择对应 JSON 协议，不再根据渠道域名误判新旧分组；支持本地素材转换、MiniMax H3 首尾帧/音频约束、任务恢复与失败原因展示。
+ [新增] 提供 Toonflow 供应商视频服务文件，按模型 ID 自动选择 772 旧/新协议，并支持分别配置旧分组与新分组 API Key。
+ [新增] Toonflow 772 供应商增加 `deepseek-v4-flash`、`deepseek-v4-pro`、`gpt-5.6-sol`、`gpt-5.6-terra` 文本模型和 `gpt-image-2` 图片模型，版本更新至 2.1。
+ [修复] 修正画布节点复制与粘贴时生成 ID 的模板字符串语法，恢复新建画布页面加载。
+ [修复] `grok-imagine-video-1.5-preview` 按模型名单独使用 `/v1/videos` 和 `input_reference` 提交图生视频，避免误请求 `/v1/videos/generations`。
+ [适配] `GLM-Image` 使用七组固定图片尺寸，工作台与画布按模型同步尺寸选项、摘要和请求参数。
+ [修复] Gitee GLM-Image 按无生成校验结果映射七档合法 `size`，例如 `9:16` 提交 `576x1024`，避免参数错误或输出回落为方图。
+ [新增] GLM-Image 图片设置增加 1-50 生成步数，默认推荐 9，并同步至画布节点、生成记录和 API 请求。
+ [修复] GLM-Image 改用 FreeImg/Gitee Serverless 的 `width`、`height`、`num_inference_steps` 调用方式，恢复 2K 尺寸与非 1:1 比例。
+ [适配] `z-image-turbo` 复用 FreeImg/Gitee Serverless 的宽高与推理步数参数协议。
+ [优化] `z-image-turbo` 与 `GLM-Image` 共用固定 2K 尺寸选项，保持设置面板、画布和请求参数一致。

## v0.4.31 - 2026-08-14

+ [修复] 清理 Ant Design 图标依赖后，GitHub 入口改用当前 Lucide 版本提供的 Git 分支图标，恢复 Web 类型检查与生产构建。

## v0.4.30 - 2026-08-14

+ [清理] 移除静态版不可用的账号/云端渠道空实现及残留用户状态、恒为 0 的积分展示、只剩单一取值的 `channelMode` / `apiFormat` 配置字段、Vite 中无效的 `use client` 指令和未使用的前端依赖。
+ [重构] 拆分画布详情页中的脚本生成解析、媒体与生成任务工具、生成请求生命周期、历史记录与项目持久化 hook 及顶部栏组件，降低超大页面文件的职责耦合。

## v0.4.29 - 2026-08-14

+ [优化] 渠道模型拉取移入新增/编辑抽屉，按当前 Base URL 与 Key 真实请求模型列表，移除渠道列表外部的单个与批量拉取入口；同域代理返回 500-504 时自动尝试浏览器直连。
+ [修复] 异步生图任务完成后主动携带渠道鉴权拉取 `/images/generations/{task_id}/content`，内容接口不可用时再回退图片 URL。
+ [适配] `sd-2.0-720p`、`sd-2.0-1080p`、`sd-2.0-4k`、`seedance-2.5` 按 videos-4 的 `/v1/videos` JSON 创建、轮询与内容读取协议接入，并按模型限制图片/视频/音频参考素材数量。

## v0.4.28 - 2026-08-04

+ [修复] HTTPS 页面展示上游 `http://IP` 图片时改为同域 `/api/proxy` 转发，避免 Mixed Content 拦截导致画布/结果卡空白。
+ [修复] 远程图片后台落盘改为经 `/api/proxy` 拉取（规避 imgen.x.ai 等 CORS），打开画布时补写 storageKey，避免“显示成功后再打开变成加载中”。
+ [修复] 画布/生图/素材下载远程图片时先经代理拉取为 blob 再保存，避免跨域或 http 直链下载失败。
+ [优化] 本地 C-ai Agent 侧边栏画布写操作确认支持队列：连续工具调用依次排队，可逐条或全部批准/拒绝，执行中新到的操作也会入队。
+ [适配] OpenAI 兼容渠道接入文档图片模型：`gpt-image-2` 文生图 `/images/generations` 与参考图编辑 `/images/edits`（`image[]` 失败回退 `image`），支持 1K/2K/4K 与宽高比尺寸映射；`gemini-3-pro-image-preview` / `gemini-3.1-flash-image-preview` 按模型名走 Gemini 生图并提交 `imageConfig`。
+ [优化] 异步图片任务返回 `task_id` 时轮询 `/images/generations/{task_id}`。
+ [修复] 视频参考图若已是公网 HTTPS URL 时不再误走已移除的本地上传接口；本地素材仍明确提示需公网链接。
+ [优化] 视频/图片请求在同域代理 403/408/502/504/520-524（含 Cloudflare 超时）时自动直连兜底。
+ [优化] OpenAI 格式 Grok Imagine 视频（含 1.5）参考图优先传公网 HTTPS URL；本地图过大时压缩为 JPEG 再提交，降低 `/videos/generations` 经代理 524 超时。
+ [新增] 开发调试日志：配置页「生成偏好」可开启；记录视频/图片请求路径、状态码、payload 大小、代理回退与错误摘要，支持复制/清空，右下角悬浮入口查看。
+ [新增] Docker 版恢复参考素材临时上传：`POST/GET /api/uploads/references`，支持图片/视频/音频，默认 15 天清理；需配置 `C_AI_PUBLIC_BASE_URL` 并挂载上传目录。
+ [优化] Grok/Seedance 等本地参考素材优先上传为公网 HTTPS 直链再提交上游，减少 base64 大包与 524。
+ [新增] 接入 videos-4 系列视频模型（`videos-4`、`-fast`、`-mini` 及各自 480p/720p 变体共 9 个）：按 JSON 版 `POST /v1/videos` 提交 `duration`/`ratio`/`resolution`/`referenceImages`/`referenceVideos`/`referenceAudios`，轮询 `GET /v1/videos/{task_id}` 间隔 5 秒，完成后读 `/content` 或 `video_url`。模型名自带分辨率时以模型名为准并锁定设置项；时长限制 4-15 秒；支持参考图/视频/音频（各 9/3/3 个，需公网 HTTPS），首尾帧按文档明确报错。配置页可一键填入这 9 个模型。
+ [变更] 渠道调用格式收敛为仅 OpenAI：移除 Gemini、火山方舟 Seedance、Cai、Cai 二号、NewToken、Duomi、Lingdong 七种格式及其专属请求/轮询实现和内置模型列表；配置页不再有「调用格式」下拉，已保存渠道回落为 OpenAI 并保留 Base URL、Key 和模型列表。`gpt-image-2`、Gemini 生图预览、Grok Imagine、Seedance、`veo-3-1` 等仍按模型名识别，参数面板不变。
+ [新增] 提示词来源支持配置与定时拉取：内置六个 ImagePrompts 统一 JSON 来源，可单独启用/停用、查看内容、立即拉取，并支持新增自定义 JSON 来源；配置页新增「提示词来源」Tab，可设定拉取周期，页面打开期间按周期自动更新已启用来源。提示词中心改为按来源缓存取数，来源失败时保留上一次成功内容。
+ [新增] 配置与用户偏好支持导入导出 JSON 文件，包含渠道、默认模型、生成偏好和 WebDAV 配置；文件含 API Key 与 WebDAV 凭据，不应公开分享。
+ [优化] 渠道编辑改为独立抽屉：列表卡片只显示名称、Base URL 和模型数量，点「编辑」或「新增渠道」在抽屉里维护渠道并在保存时统一写回，不再逐字符改动配置。
+ [优化] 画布右侧 Agent 面板宽度改为持久化保存（320-760），重新打开画布后保留上次拖拽的宽度。
+ [修复] 拖动节点四角缩放时暂停渲染跟随节点的浮层工具条，避免高频更新导致页面崩溃。
+ [变更] 画布节点类型放开为字符串并由注册表提供创建规格，为后续节点插件预留 `<pluginId>:<name>` 类型；未注册类型渲染为「未知节点」占位而不影响其他节点。
+ [优化] 移除顶栏「本地」按钮，配置入口统一走齿轮按钮，并去掉顶栏每次挂载都会发起的账号查询请求。

## v0.4.27 - 2026-07-10

+ [修复] 同域 AI 代理同源校验兼容反代/Vite 开发转发后的 Host 差异，避免生图请求误报 `origin is not allowed` 或被前端兜底成鉴权失败。
+ [优化] 同步生图结果优先 b64 快速转 Blob 立即展示，完整落盘后台进行；兼容上游只返回 b64 或 URL 的多种字段结构。
+ [修复] 回退不稳定的 `response_format=url` 强求，避免中转返回空/不可拉取 URL；b64 解码去空白并兼容 url-safe，避免“拉取不到图片”。
+ [适配] `grok-imagine-image-lite` 图片尺寸与 `grok-imagine-image` 对齐，支持相同 `1k/2k` 与宽高比设置。
+ [优化] Grok Imagine 图片尺寸面板按模型名动态切换，不依赖渠道 `apiFormat`，切换模型后设置组件与摘要标签同步刷新。
+ [新增] 画布输入框魔法棒接入文本模型：有提示词时优化文案；无文字但有参考图时反推图片提示词。
+ [修复] 魔法棒改为非流式 chat/completions（失败再回退 /responses），并增加 90s 超时，避免一直转圈无结果。
+ [修复] 生图工作台成功记录缩略图：b64 落盘后再返回 storageKey，历史列表按 storageKey 解析显示，避免 blob 失效导致空白。
+ [优化] Grok Imagine 原生生图优先 `response_format=url`，解析兼容 URL/b64 与多种返回结构；审核失败时展示上游 `error` 原文，避免上游已出图本地仍卡住。
+ [优化] 生图拿到远程 URL 后立即展示，再在空闲时后台下载到本地 IndexedDB，并回写 storageKey 供历史缩略图使用。

## v0.4.26 - 2026-07-09

+ [修复] 清理 Web 旧 `pnpm-lock.yaml`，避免残留 Next / Ant Design Pro 依赖误导安装和审计。
+ [修复] 修正提示词中心多处无效 Tailwind 色阶 class，保证筛选、详情和卡片 hover 样式正常生成。
+ [加固] Go 同域 AI 代理增加同源请求校验，并在实际拨号前再次解析和拒绝私网 IP，降低跨站滥用和 DNS 复绑定风险。

## v0.4.25 - 2026-07-09

+ [修复] 增加路由错误兜底，动态懒加载 chunk 因发版后文件名变化加载失败时自动刷新页面，避免视频工作台显示 React Router 默认错误页。
+ [修复] Docker nginx 对入口 HTML 和 SPA 回退响应增加 `no-store`，并为缺失的旧 JS chunk 返回自动刷新模块，降低浏览器或 CDN 持有旧入口导致的动态资源 404。

## v0.4.24 - 2026-07-09

+ [修复] 新旧本地配置默认切到同域 AI 代理，避免升级后仍直连上游导致图片接口 CORS 失败。
+ [优化] 延迟加载配置弹窗、移除未使用的 Ant Design Pro 依赖，Docker nginx 开启 gzip，并避免生图记录列表初始化时批量读取历史图片，降低首屏和生图页卡顿。

## v0.4.23 - 2026-07-08

+ [新增] 增加 Go 同域 AI 请求代理 `/api/proxy`，Docker 镜像同时运行 nginx 静态站点和 Go 代理，静态前端可在配置中切换浏览器直连/同域代理以规避上游 CORS 限制。

## v0.4.22 - 2026-07-08

+ [修复] 移除 Vite 手动 vendor 分包，修复线上 Ant Design 依赖初始化时报 `useLayoutEffect` 为空导致首页空白的问题。

## v0.4.21 - 2026-07-08

+ [调整] 主应用从 Next.js 迁移为 Vite 静态前端构建，使用 React Router 承载现有页面，并改为 nginx 静态镜像运行。
+ [调整] 移除主应用内置 Next.js API Routes / server 目录，账号云端渠道、AI 请求代理、服务端临时上传和 WebDAV 转发在静态版不可用；提示词改为浏览器直拉 GitHub Raw 并本地缓存。

## v0.4.20 - 2026-07-03

+ [新增] 适配 Grok Imagine 图片/视频新模型，图像设置按 `1k/2k`、宽高比和 `aspect_ratio` / `resolution` 动态提交，视频设置按模型能力切换比例、清晰度和参考图模式。
+ [新增] 适配 StepFun `step-image-edit-2` 固定尺寸与专用提交参数，生图工作台和画布使用同一份归一化配置。
+ [修复] Grok 图片结果优先使用 `b64_json`，并增强远程图片转存、代理读取和渲染失败兜底，避免“URL 可打开但工作台或画布显示失败”。
+ [修复] 清理 Ant Design 静态 `message`、Tooltip 旧属性和主题初始化脚本问题，减少 Next.js / antd 控制台告警。
+ [优化] 补齐前端类型检查阻断项，保证当前 Web 工程 `tsc --noEmit` 可通过。

## v0.4.19 - 2026-06-30

+ [修复] 同步图片代理任务改用 Node 原生 HTTP 客户端在服务端后台请求上游，避免 Next `fetch` / 请求生命周期取消长耗时上游连接导致上游 `context canceled`。

## v0.4.18 - 2026-06-30

+ [修复] 页面和接口响应增加 no-store 缓存控制，并让旧版同步生图代理请求快速提示刷新，避免浏览器或 CDN 继续使用旧前端导致 524。

## v0.4.17 - 2026-06-30

+ [修复] AI 请求代理转发同步图片生成/编辑时改为本地代理任务轮询，避免上游已成功但本地长连接被边缘网关 524 截断。

## v0.4.16 - 2026-06-29

+ [新增] 渠道调用格式新增 Lingdong，内置灵动模型列表，并适配其图片生成和异步视频创建/查询协议。

## v0.4.15 - 2026-06-28

+ [优化] 异步视频任务轮询到上游公开视频 URL 后，画布和视频创作台直接显示结果，不再等待前端完整下载视频文件，减少上游已成功后的页面延迟。
+ [新增] 补充服务器本地构建升级脚本，便于部署环境按当前代码重新构建 Docker 镜像并刷新缓存。

## v0.4.14 - 2026-06-25

+ [修复] 生图/视频工作台生成中或上游失败的记录支持停止本地任务并删除，画布删除节点时也会中断关联生成请求，避免异步任务完成后把已删除内容写回。

## v0.4.13 - 2026-06-25

+ [优化] NewToken 图片、视频和音频参考素材改为通过服务端代理上传到 `aimh8` 媒体接口，默认内置上传 Key，并统一使用返回的 OSS 公网 URL 提交给上游。

## v0.4.12 - 2026-06-25

+ [优化] 视频创作台左侧生成记录补充生成时间显示，便于按时间回看历史任务。

## v0.4.11 - 2026-06-25

+ [修复] 云端个人渠道保存 Key 支持在服务端数据目录自动生成并持久化加密密钥，Docker 默认挂载账号数据目录，避免登录后保存渠道因缺少密钥或容器重启而失效。
+ [修复] 在线 Agent 工具对话在上游不兼容 Responses 工具 schema 时自动降级到 Chat Completions 工具格式，避免出现 `Bad input` 导致无法聊天。
+ [优化] 默认 Docker Compose 部署改为使用本地 Dockerfile 编译镜像，线上更新可直接从当前代码构建，不再依赖远端 latest 镜像缓存。

## v0.4.10 - 2026-06-25

+ [优化] 生图和视频提交前会将 `@图片1`、`@视频1`、`@音频1` 规范为编号文本，并在提示词前追加参考素材编号说明，确保上游按实际素材字段理解引用关系。
+ [优化] Cai、NewToken、Duomi 和 Seedance 视频参考素材提交前强制转换为公网 HTTPS URL，并通过 `HEAD` 或 `GET Range` 自检可读性，失败时前端直接拦截。
+ [优化] 调用格式、云端渠道、Agent 面板和鉴权错误提示统一去掉 API 字样，保留更简洁的 Cai、NewToken、Duomi 和 Key 表达。

## v0.4.9 - 2026-06-24

+ [优化] 视频创作台提示词框支持 `@` 引用已添加的参考图片、视频和音频素材，并继续随请求传给视频任务。
+ [优化] 画布、生图工作台和视频工作台的 `@` 引用按钮避免触发输入框失焦，候选菜单点击后稳定弹出。
+ [新增] 视频创作台生成记录支持首帧缩略图，加入“我的素材”的视频也会带封面并在刷新后恢复。
+ [优化] 配置弹窗调用格式去除“官方接口 / 第三方接口”分组，并将 Cai、NewToken、Duomi 等展示名去掉 API 字样。

## v0.4.8 - 2026-06-24

+ [优化] 画布生成输入框点击 `@` 按钮会按当前光标位置打开引用素材候选菜单，不再需要手动输入 `@`。
+ [优化] 画布视频节点连接音频后会在参考缩略图中显示音频图标，便于确认音频素材已连接。
+ [优化] 生图工作台提示词框支持 `@` 引用已添加的参考图，并在提交时继续把参考图作为图片文件传给上游。

## v0.4.7 - 2026-06-23

+ [新增] 画布脚本生成器支持“主题分镜 / 参考图文案 / 图文转视频”三种模式，支持上传参考图后自动连线，并在生成结果中保留文案分析摘要。
+ [优化] Cai / NewToken / duomiapi 视频参考图片提交时统一重新上传到本服务临时存储，不再直传原始外部图片 URL。
+ [优化] 参考素材临时上传接口补充 `HEAD`、`Content-Length`、`Accept-Ranges` 等响应头，并在提交上游前自检公网 URL 可读性；当反向代理未返回 `Content-Length` 或拦截 `HEAD` 时，会改用 `GET Range` 探测兜底。
+ [优化] 生图工作台和视频创作台支持已有任务生成中继续提交新任务，历史记录保留每次提交，视频任务可并行后台轮询。
+ [优化] Cai API 的 `videos` / `videos_stable` 按 Seedance 能力处理，支持首尾帧、全能参考和参考视频，并在配置页提供 Cai 视频模型一键填入。
+ [修复] `grok-imagine-video-1.5-preview` 在 Cai 专用接口、NewToken 和 duomiapi 下统一使用 `input_reference` 提交参考图，并在未连接图片时先给出本地提示。
+ [修复] 修复视频创作台残留旧 `setRunning` 状态调用导致并发提交状态不完整的问题。
+ [修复] 全能参考模式补充音频单独使用校验，并统一支持模型的提示文案，避免误提交无效参考组合。

## v0.4.6 - 2026-06-22

+ [新增] NewToken `gpt-image2-1k` / `gpt-image2-2k` / `gpt-image2-4k` 改为按官方异步图片任务协议走 `/v1/videos` 创建和轮询，`*_sync` 模型继续使用同步图片接口。
+ [优化] 画布视频提交与视频创作台校验对齐，统一空提示词拦截、参考素材校验、比例/清晰度/秒数/声音/水印参数归一化。
+ [优化] Cai / NewToken / duomiapi 视频参考图片、视频和音频在提交前会上传为公网 HTTPS URL，减少本地 `blob:` / dataURL 导致的上游超时。
+ [优化] 画布缩小时保持生成输入面板可读，并提高视频输入区最小高度。
+ [修复] 修复画布视频提交时误用未定义变量导致 `node is not defined` 的问题。
+ [修复] 视频最大生成时长统一限制为 15 秒，避免提交上游不支持的更长时长。

## v0.4.5 - 2026-06-20

+ [新增] 渠道调用格式新增 NewToken 异步接口，支持常用 NewToken 视频模型一键填入，并按模型族适配异步视频创建、轮询和参考素材字段。
+ [优化] 配置页去除 NewToken 底层协议说明文案，保持调用格式选择区简洁。

## v0.4.4 - 2026-06-20

+ [优化] 画布视频生成面板按 C-AI Apifox 文档动态展示模型能力，Seedance 支持首尾帧和全能参考，Grok 模型仅展示其支持的文生/图生/图片参考能力。
+ [优化] Cai 视频任务 payload 按模式拆分首尾帧、全能参考和普通图片参考，避免多图参考被误判为首尾帧。
+ [修复] 修复视频生成输入面板在参数较多或模型名较长时底栏遮挡输入框的问题，并补齐图片/视频参数弹层在配置节点中的全宽样式。

## v0.4.3 - 2026-06-20

+ [新增] 画布自动整理新增视口自动对焦（Auto-Focus）功能，一键将整理后的树状推演图缩放居中于屏幕。
+ [新增] 提示词面板集成魔法棒一键优化提示词（🪄）功能，可自动丰富高级电影级细节修饰词。
+ [优化] 提示词面板底部控制栏像素级重构：大圆角纯白微阴影卡片设计，平铺无边框输入区，并压缩为极简单行底栏。
+ [优化] 视频参数设置和图片参数设置重构为组合胶囊按钮（如视频的比例、时长、清晰度组合展示；图片的清晰度、比例组合展示），极大扩展可用宽度。
+ [优化] 画布自动整理拓扑间距调优，将列间距增大至 `280` 像素，行间距增大至 `100` 像素，避免复杂拓扑折线重叠。
+ [优化] 画布视频连线流动特效 (Flowing Glow) 以及输入框上方参考卡片 Hover 遮罩删除交互。

## v0.4.2

+ [新增] 视频生成接入 Cai 专用接口，按模型类型适配 `/v1/videos` 与 `/v1/video/generations` 异步创建和轮询路径。
+ [新增] 画布视频节点保存异步任务 ID，超时、刷新或网络中断后可手动拉取已完成的视频结果。
+ [新增] 增加 AI 请求代理，拉取模型、渠道诊断、生图、音频和视频请求可通过 Next.js 服务端转发，降低跨域和 403 问题。
+ [新增] 首页增加动态视觉效果和画布节点演示墙，强化 C-AI 画布生成、连接和连续推演的产品表达。
+ [优化] 视频创作台和画布视频任务轮询最长等待时间调整为 30 分钟，适配长耗时上游任务。
+ [优化] 渠道设置中原 OpenAI JSON 调用格式调整为 Cai 专用接口展示，并优化视频超时与公网素材 URL 提示。
+ [修复] 修复视频节点生成成功后无法拖动、播放时误触发节点拖拽的问题。

## v0.4.1 - 2026-06-18

+ [新增] 渠道兼容 Gemini 格式。
+ [新增] 无限画布图片节点支持复制为 PNG 到系统剪切板。
+ [新增] 视频生成支持通用 OpenAI JSON 异步协议，彻底兼容三方中转代理服务。
+ [优化] 提示词中心全新 UI 视觉重构：设计了微光渐变头部，重构了分类分段按钮和胶囊标签过滤，清洗并合并繁杂作者标签。
+ [优化] 提示词详情弹窗视觉重构：自定义头部设计，输出预览 Terminal 代码框，提升信息层级。
+ [修复] 修复画布输入框聚焦反应与输入光标 Caret 在 Chrome/Safari 下闪烁消失的问题。
+ [修复] 修复画布生成控制栏当模型名或参数文字较长时与停止按钮布局重叠的 Bug。

## v0.4.0 - 2026-06-16

+ [新增] 新增网页版Agent Loop模式。
+ [新增] 支持Vercel一键部署。
+ [调整] 移除后端，项目定位为个人画布工具。

## v0.3.0 - 2026-06-15

+ [新增] 新增 C-ai Agent 通过 Codex 操作画布。

## v0.2.5 - 2026-06-08

+ [新增] 新增图片切图功能。
+ [新增] 支持webdav同步数据。
+ [修复] 修复画布文字节点错误问题。

## v0.2.4 - 2026-06-04

+ [新增] 新增图片反推提示词功能。

## v0.2.3 - 2026-06-04

+ [新增] 新增图片蒙版局部修改功能。
+ [优化] 优化配置节点@图片功能。

## v0.2.2 - 2026-06-04

+ [新增] 新增图片放大工具。
+ [优化] 优化图片工具条，增加自定义功能。
+ [修复] 修复端口冲突问题、pg/mysql未初始化问题。

## v0.2.1 - 2026-06-03

+ [新增] 新增文档站点页面。
+ [优化] 优化画布连线交互。
+ [优化] 优化模型选择用户偏好。

## v0.2.0 - 2026-06-01

+ [新增] 支持通过火山方舟AgentPlan接入。
+ [新增] 视频生成支持声音、水印及图片/视频/音频参考输入。
+ [新增] 画布新增音频节点。
+ [优化] 图片/视频素材支持 `图片1`编号注入提示词。

## v0.1.1 - 2026-05-30

+ [新增] 支持New API跳转并自动填入Base URL和API Key配置。

## v0.1.0 - 2026-05-26

+ [优化] 优化我的画布、我的素材导出功能
+ [修复] 修复画布撤销，配置节点等bug问题

## v0.0.9 - 2026-05-26

+ [新增] 新增视频创作台页面。
+ [修复] 修复图片节点size参数传递问题。

## v0.0.8 - 2026-05-24

+ [新增] 新增用户账号与算力点体系，支持账号密码注册登录、Linux.do OAuth。
+ [新增] 管理后台公开配置支持设置模型算力点、支持计费查询。
+ [新增] 画布右上角展示用户算力点余额，生成按钮会展示本次预计消耗算力点。
+ [新增] 新增视频生成节点。

## v0.0.7 - 2026-05-23

+ [新增] 管理后台提示词管理支持多选批量删除。
+ [新增] 新增定义拉取GitHub提示词源功能。
+ [新增] 新增awesome-gpt-image2-prompts提示词来源。
+ [优化] 优化模型下拉选择样式、优化生图编辑设置

## v0.0.6 - 2026-05-22

+ [新增] 管理后台支持配置模型渠道，前端当前无需鉴权即可直接使用后端渠道能力。
+ [优化] 统一整理后端错误提示、AI 代理、图片节点生成与重试、参考图缺失处理等细节。
+ [优化] 后端模型代理路径调整为 OpenAI 风格。

## v0.0.5 - 2026-05-20

+ [新增] 右上角版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和按时间线整理的更新日志。
+ [新增] 设置弹窗支持配置系统提示词，AI 生图、编辑图和文本请求会自动携带。

## v0.0.4 - 2026-05-20

+ [调整] Docker 运行入口改为 Next.js 对外提供页面，`/api/*` 由 Next.js 代理到内部 Go 服务。
+ [修复] 文本复制在局域网 IP 访问时可能失败的问题。

## v0.0.3 - 2026-05-19

+ [修复] 更新 nanoid 依赖并修改 ID 生成方式，防止其他ip无法使用crypto模块导致的ID生成失败问题。

## v0.0.2 - 2026-05-19

+ [新增] 增加生图工作台功能，支持文生图、图生图、查看历史记录，并增加移动端适配。
+ [修复] 画布生成尺寸控件支持选择更多常用比例，并可直接输入自定义比例。
+ [修复] 生成配置节点恢复拖拽操作，避免面板控件拦截整块节点拖动。
+ [文档] 增加 Render 部署说明。

## v0.0.1 - 2026-05-19

+ [新增] 首次开源版本，包含无限画布能力：多画布项目、节点拖拽缩放、连线、小地图、撤销重做、导入导出。
+ [新增] AI 创作能力：支持 OpenAI 兼容接口的文生图、图生图、参考图编辑和文本问答。
+ [新增] 画布助手能力：支持围绕选中节点和上游节点对话、生图，并把结果插回画布。
+ [新增] 提示词库能力：抓取多个 GitHub 开源项目，按案例整理数百个图片提示词。
